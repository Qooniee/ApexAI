# ===============================================================
# Informer Model Configuration for Vehicle Identification
# ===============================================================


name: "Informer"
type: "Informer"  # Model type identifier

# ==========================================
# 📊 Model Architecture
# ==========================================
architecture:
  # Informer hidden state dimension
  hidden_dim: 128

  # Number of stacked Encoder layers
  num_layers: 2

  # Number of Multi-Head Attention heads
  num_heads: 4

  # Feed Forward Network dimension multiplier
  # Actual FFN dimension is calculated as hidden_dim * feedforward_multiplier
  # Standard value: 4 (following Transformer paper. If hidden_dim=128, then FFN=512)
  feedforward_multiplier: 4

  # Dropout rate
  dropout_ratio: 0.1

  # Output dimension (number of classification classes)
  out_dim: 21

  # Number of classification classes
  num_classes: 21

  # Input sequence length
  seq_len: 100

  # Expected feature dimension
  feature_size: 9  # pbrake_f, pbrake_r, Steering_Angle, accx_can, accy_can

  # Classification task flag
  classification: true
